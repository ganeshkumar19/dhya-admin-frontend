export enum SupportStatus {
  OPEN = 'Open',
  IN_PROGRESS = 'In_progress',
  RESOLVED = 'Resolved',
  CLOSED = 'Closed',
}

export enum SupportSeverity {
  LOW = 'Low',
  MEDIUM = 'Medium',
  HIGH = 'High',
  CRITICAL = 'Critical',
}
